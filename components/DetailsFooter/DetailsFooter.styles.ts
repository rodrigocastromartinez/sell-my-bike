import { StyleSheet } from "react-native";
import { tokens } from "@/styles/tokens";

export const styles = StyleSheet.create({
  text: {
    fontSize: tokens.spacing.m,
    // marginVertical: tokens.spacing.xxs,
  },
});
